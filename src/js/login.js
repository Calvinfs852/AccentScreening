function verifyAndLogin(password)
{
    if(true/*authenticate against stored admin password*/, window)
    {
        window.location = "homepage.html";//take to admin homepage
        alert("Password verified");
    }
}
